from datetime import datetime

# paths to backup
source_paths = [
    r'C:\My Source Directory',
    r'C:\A Text File.txt',
]

# temporary location to store archive before uploading
temp_path = r'C:\tmp'

# name of the archive
archive_name = f"REPLACE ME{datetime.now().strftime('%Y%m%d_%H%M%S')}.7z"

# password of the archive
password = 'REPLACE ME' # replace this with a password applied ot the 7z archive

# Dropbox info
dropbox_folder = "/backups" # folder to place archives in
dropbox_app_key = "REPLACE ME"
dropbox_app_secret = "REPLACE ME"
dropbox_refresh_token = "REPLACE ME" # run get_token.py

# login and go to https://www.dropbox.com/oauth2/authorize?client_id={dropbox_app_key}&token_access_type=offline&response_type=code
dropbox_access_code = "REPLACE ME"
